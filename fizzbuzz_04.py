import sys


# if sys.var[1] is empty (user has not called a var), use int(1)
# if sys.var[1] returns a #, convert to int and begin fizzbuzz routine from there

if sys.argv[1]

# this is my latest github attempt

# and now this line is created to see if
# git & github recognize this new version
